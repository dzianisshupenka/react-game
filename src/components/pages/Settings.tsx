import React from 'react';

const Settings:React.FC = () => {
    return (
        <div className="settings-main">
            <h1>In progress..</h1>
        </div>
    );
};

export default Settings;